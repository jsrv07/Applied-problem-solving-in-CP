class Solution {
public:
    double findMedianSortedArrays(vector<int>& nums1, vector<int>& nums2) {
        double l=0,h;
        int n1=0,n2=0,sn=nums1.size(),sm=nums2.size(),s1,s2,cnt=0;
        h=(s1+s2)/2;
        if(int(h)==h) {s1=h-1;s2=h;}
        else {s1=int(h);s2=int(h);}
        while(n1<sn && n2<sm){
            
        }

    }
};